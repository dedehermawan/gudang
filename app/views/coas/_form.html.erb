<%= form_for @coa, remote: true, html: {styles: "display:inline;"} do |f| %>
  <div class="modal-body">
    <ul class="errors"></ul>
    <div class="form-group">
      <%= f.label :coa_id, class: 'control-label' %>
      <%= f.text_field :coa_id, class: 'form-control first-input' %>
    </div>
    <div class="form-group">
      <%= f.label :coa_name, class: 'control-label' %>
      <%= f.text_field :coa_name, class: 'form-control' %>
    </div>
  </div>

  <div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
    <button type="submit" class="btn btn-primary"><%= yield(:button_text) %></button>
  </div>
<% end %>
